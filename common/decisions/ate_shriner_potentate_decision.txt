# Seize the Imperial Divan — become Imperial Potentate of the Shriner faith
ate_shriner_imperial_potentate = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_faith.dds"
	}
	decision_group_type = major

	ai_check_interval = 60

	desc = ate_shriner_imperial_potentate_desc
	selection_tooltip = ate_shriner_imperial_potentate_tooltip

	is_shown = {
		always = no
	}

	is_valid = {
		OR = {
			# Path A — Emperor who has ruled long enough
			AND = {
				any_held_title = { tier = tier_empire }
				years_as_ruler >= 10
			}
			# Path B — Independent ruler of 3 kingdoms with sufficient piety
			AND = {
				is_independent_ruler = yes
				any_held_title = {
					count >= 3
					tier = tier_kingdom
				}
				piety >= 500
			}
		}
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	cost = {
		gold = 1000
	}

	effect = {
		trigger_event = { id = ate_shriner.0003 }
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 5
	}
}
